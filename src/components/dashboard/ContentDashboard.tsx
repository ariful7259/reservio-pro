
                  <Progress 
                    value={type.value} 
                    className="h-2" 
                    style={{ backgroundColor: `${COLORS[index % COLORS.length]}20` }}
                  />
